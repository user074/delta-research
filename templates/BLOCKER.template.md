# BLOCKED — (run ID): (experiment question)

> This is an execution note, not a research report or completed run. Keep the same run ID when work resumes.

- **exact blocker:** (plain-English statement)
- **error or missing resource:** `(exact error, path, permission, quota, or unavailable dependency)`
- **repairs attempted:** (up to three concrete attempts and outcomes)
- **why the experiment cannot answer yet:** (which required comparison/condition is missing)
- **required resolution:** (one prerequisite that would allow the same PLAN.md to continue)
- **resume command:** `(exact command, if known)`
- **recording rule:** No `REPORTS/R###.md`, Ledger row, belief update, `total_runs` increment, or new run ID.
